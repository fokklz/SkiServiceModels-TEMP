# SkiServiceModels - BSON.AutoMapper

A extension for the SkiServiceBson library that implements a AutoMapper profile preconfigured for the models of the library.

```csharp
builder.Services.AddAutoMapperProfile();
```